export enum Paths {
  HOME = '/',
  CREATE = `${HOME}create`,
  TICKET = `${HOME}tickets`,
  TICKET_ID = `${TICKET}/[id]`,
  CUSTOMER = `${HOME}customers`,
  CUSTOMER_ID = `${CUSTOMER}/[id]`,
  FORM = `${HOME}forms`,
  FORM_ID = `${FORM}/[id]`,
  GUIDE = `${HOME}guides`,
  GUIDE_ID = `${GUIDE}/[id]`,
  SOFTWARE = `${HOME}software`
}
